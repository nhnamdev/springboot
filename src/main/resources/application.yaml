server:
  port: 8080
  servlet:
    context-path: /migi


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/migi_services"
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true